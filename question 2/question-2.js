const arr1 = ["-1-", "*2*", "-3-", "*4*", "-5-"];
const arr2 = ["*1*", "-2-", "*3*", "-4-", "*5*"];


// as length of both the will be same
for (let i = 0; i < arr1.length; i++) {
  console.log(arr1[i] + ',' + arr2[i]);
}